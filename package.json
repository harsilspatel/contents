{
  "name": "cuntents",
  "version": "1.0.0",
  "description": "an app to auto generate toc for markdown",
  "author": "harsilspatel <harsilspatel@gmail.com> (https://github.com/harsilspatel/cuntents)",
  "license": "ISC",
  "repository": "https://github.com/harsilspatel/cuntents.git",
  "homepage": "https://github.com/harsilspatel/cuntents",
  "bugs": "https://github.com/harsilspatel/cuntents/issues",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "scripts": {
    "dev": "nodemon",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "probot": "^7.2.0"
  },
  "devDependencies": {
    "jest": "^24.0.0",
    "nock": "^10.0.0",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.2",
    "standard": "^12.0.1"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "nodemonConfig": {
    "exec": "npm start",
    "watch": [
      ".env",
      "."
    ]
  },
  "jest": {
    "testEnvironment": "node"
  }
}
